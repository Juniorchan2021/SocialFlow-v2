# Anthropic Claude API Key (required for AI features)
# Get yours at: https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-xxx

# Without this key, the tool still works with rule-based detection only.
# AI deep analysis, image analysis, and smart rewrite require this key.
